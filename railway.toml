[build]
# Use the production Dockerfile at repository root (not docker/app/Dockerfile)
dockerfilePath = "Dockerfile"

[deploy]
# Railway injects $PORT; the entrypoint uses it
healthcheckPath = "/up"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
